const API_PREFIX: string = '/api';

export const COMMENTS: string = `${API_PREFIX}/comments`;
export const USERS: string = `${API_PREFIX}/users`;
export const SIGN_UP: string = `${API_PREFIX}/sign-up`;
